info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:50000/auth/login
  body:
    type: json
    data: |-
      {
        "email": "test2@test.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
